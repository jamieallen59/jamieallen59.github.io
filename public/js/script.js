import React from 'react';
import ReactDOM from 'react-dom';

let Tabs = React.createClass({
  render: () => {
    return (
      <div>YES</div>
    )
  }
})

ReactDOM.render(<Tabs />, document.getElementById('my-component'));
